# heart-decease-prediction
it will predict whether a patient is deceased or not

## Run locally

1. Start the FastAPI backend with `python main.py`
2. Start the Streamlit frontend with `streamlit run app.py`
